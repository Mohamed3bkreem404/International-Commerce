apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana

data:
  datasource.yml: |
{{ .Files.Get "grafana/datasources/datasource.yml" | indent 4 }}
  dashboards.yml: |
  {{ .Files.Get "grafana/dashboards/dashboard.yml" | indent 4 }}
  ecommerce.json: |
  {{ .Files.Get "grafana/dashboards/ecommerce.json" | indent 4 }}