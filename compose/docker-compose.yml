include:
  - app.yml
  - databases.yml
  - proxy.yml
  - ../monitoring/docker-compose.yml

networks:
  ecommerce-net:

    driver: bridge

  database-net:
    driver: bridge

  monitoring-net:
    driver: bridge

volumes:
  payment_data:
  products_data:
  order_data:
  cart_data:
  accounts_data:
  grafana-data:
  alertmanager-data:
